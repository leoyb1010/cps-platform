# 商业化前深度评审与升级计划

> 评审日期:2026-07-05 · 基线:main @ e34e40e(两轮缺陷 review 已合并)
> 方法:四个并行审计维度(工程就绪度 / 产品旅程 / 设计系统 / 动效交互),全部结论经主线逐条回代码核实后才收录;审计过程中修正/剔除的误报不在本文出现。
> 代码规模:前端 85 文件 / 1.7 万行 / 60 页面;后端 38 文件 / 4300 行;测试 35(FE)+ 146(BE)全绿。

---

## 0. 执行摘要

| 维度 | 成熟度 | 一句话判断 |
|---|---|---|
| 工程/生产就绪 | ★★★☆☆ | 可观测性/限流/幂等/审计底子远超演示项目;**数据生命周期(迁移+备份)与默认凭据是硬伤** |
| 产品旅程 | ★★☆☆☆ | 演示态打磨完整;**"真实模式"只是写路径接了后端,读侧大量假数据直出** |
| 设计系统 | ★★★★☆ | 令牌架构(暗色/白标/焦点环)已商业级;债务是可枚举清扫项(字阶/手搓按钮/灰字对比度) |
| 动效交互 | ★★★☆☆ | 入场储备扎实、C 端购买流上半程达标;**系统性短板:只有入场没有退场、参数未 token 化** |

**核心结论:这不是一个"还差很多功能"的产品,而是一个"演示动脉粗、商业动脉细"的产品。** 卡商业化的是 7 个 P0(见 §1),总工作量约 5–8 人日,建议第一周内全部闭环;P1 约 2–3 周一个迭代清完;设计/动效升级可与 P1 并行,均为渐进式,无需推倒任何架构。

---

## 1. P0 —— 商业化阻断(7 项,不修不能对真实商户开放)

### 工程侧(3 项)

| # | 缺口 | 证据 | 动作 | 工作量 |
|---|---|---|---|---|
| P0-1 | **生产空库自动灌演示口令**:entrypoint 空库即跑 seed,产生一批密码为 `demo` 的管理员/审计账号 | `server/docker-entrypoint.sh:16-22` + `server/prisma/seed.ts:184` | 自动 seed 加 `SEED_DEMO=true` 显式开关;生产 bootstrap 仅建首个 admin,密码从 env 注入或随机生成打印一次 | 0.5d |
| P0-2 | **无迁移历史,生产启动跑 `db push --accept-data-loss`**:任何 schema 变更可静默清空生产资金数据且不可回滚 | `server/docker-entrypoint.sh:13`;`server/prisma/` 无 migrations 目录(实测确认) | `prisma migrate dev --name init` 建基线;entrypoint 改 `migrate deploy`;`--accept-data-loss` 仅限本地 | 0.5d |
| P0-3 | **资金库零备份**:SQLite/PG 落 Docker 托管卷,全仓无备份脚本、无恢复文档、无演练 | `docker-compose.yml:18-19,36-37`;`scripts/` 仅截图脚本(实测确认) | 定时 dump(SQLite `.backup` / `pg_dump`)+ 异地存储 + `docs/backup-recovery.md` + 上线前恢复演练一次 | 1d |

### 产品侧(4 项)

| # | 缺口 | 证据 | 动作 | 工作量 |
|---|---|---|---|---|
| P0-4 | **真实模式登录页泄露演示口令 + 全系统无改密**:演示账户芯片和"密码 demo"提示无 `!isRealApi` 门控(对比 PortalLogin.tsx:87 有);全库无任何改密端点/UI(grep 零命中,实测确认);邀请建号发的临时密码永远改不了 | `src/pages/Login.tsx:76-93`;`server/src/members/members.controller.ts:99-105` | ①登录演示区加 `!isRealApi`;②新增 `POST /auth/change-password` + Profile 改密 UI + 首登强制改密;③与 P0-1 的 bootstrap 合并交付 | 1.5d |
| P0-5 | **真实模式核心 KPI 是硬编码假数**:总览流水/R-NSC/12月趋势来自 `data.ts:673` 静态常量,水合不覆盖;另有 SLA 96.2%(Complaints:76)、拦截 461 单(Risk:73)、退订完成率 96.4%(Orders:97)、续费率 63.8%(Brands:95)、归因覆盖 99.4%(Analytics:93)等字面量真实模式照常显示 | `src/lib/data.ts:673`;`src/pages/Dashboard.tsx:69-92` | 字面量指标改为派生或后端下发;算不出的显示"—/暂无数据",**绝不显示假数**。可按页分批:Dashboard → Analytics → 其余 | 2d |
| P0-6 | **后端不可达时真实模式静默展示演示数据**:水合全败时"保留本地 seed"无任何离线提示;contracts/rules/pendingAgents 真实模式永不水合,长期显示假合约/假入驻申请 | `src/lib/store.ts:279-280,188,304-308` | 真实模式水合失败挂全局 banner"服务不可达,数据可能过期";无后端表的集合真实模式置空 + "能力未接入"空态 | 1d |
| P0-7 | **C 端闭环是演示空壳**:查订阅任意输入返回 `demoProgress()`(无 isRealApi 分支,实测确认);退订只改本地 state;支付为 sandbox 模拟 | `src/pages/market/MySubscriptions.tsx:36-46`;`Supermarket.tsx:422` | 接真实支付 SDK 前 **C 端不可对外**;落地 `/market/me` 查询与退订/关闭续费端点。若首期只做 B 端,可先在 C 端挂"即将开放"门禁 | 视支付渠道 |

---

## 2. P1 —— 上线前必须(约 2–3 周迭代)

### 2.1 工程(摘自就绪度审计,均已核实)

| # | 项 | 证据 | 动作 |
|---|---|---|---|
| E1 | webhook 投递无重试/无 outbox,合作方漏收回调即两侧账务失步 | `server/src/cps/sign-webhook.service.ts:60-73`(fire-and-forget,失败仅落日志) | SignCallbackLog 加 status/attempts 当准 outbox;cron 指数退避重投 5 次;死信告警 |
| E2 | 进程内 cron 重启窗口丢准备金释放,无启动补跑 | `server/src/business/scheduled-tasks.service.ts:35-57` | 启动时补跑一次"应释未释"扫描(查询按 dueAt,天然支持 catch-up);多副本部署前上分布式锁 |
| E3 | 零错误上报/前端监控:后端异常只落本地日志,前端 grep sentry/web-vitals 零命中 | `all-exceptions.filter.ts:23`;`ErrorBoundary.tsx:14`(挂点已预留) | @sentry/nestjs + @sentry/react + web-vitals;资金异常/审计写失败必须有主动告警通道 |
| E4 | 生产无条件暴露 Swagger `/docs` 全 API 契约 | `server/src/main.ts:47-55` | production 跳过 SwaggerModule.setup 或加鉴权 |
| E5 | 后端依赖 5 high(multer DoS 链等);CI 后端 audit 不阻断 | `server/` npm audit | 升 multer ≥2.1.2 及关联包;CI 阻断标准对齐前端 |
| E6 | nginx 无 gzip、无 CSP/安全头(main.ts 注释称 CSP 交给托管层,但托管层没做) | `nginx.conf` 全文 | `gzip on`(451KB 主包裸传,一行改动收益极大)+ CSP/XCTO/Referrer-Policy |
| E7 | 后端容器 root 运行;生产镜像内置 ts-node 只为启动 seed | `server/Dockerfile`(无 USER 指令) | 加 `USER node`;seed 预编译或独立 job |
| E8 | demo RSA 私钥回退残余:NODE_ENV 未设即绕过校验;`includes('PRIVATE KEY')` 校验粘 demo 私钥也能过 | `sign-webhook.service.ts:21-23` + `main.ts:13,23` | 校验 env 私钥指纹 ≠ demo 指纹;demo-keys 按环境条件加载 |
| E9 | 时间全链路 UTC 无时区标注:订单时间甚至是展示字符串("今天 13:58"),财务对账差 8h、跨日归属错 | `seed.ts:71-84`;`Orders.tsx:196` | 后端统一 ISO8601 存储;前端集中格式化(显式 timeZone);导出列标注时区 |
| E10 | http.ts 无请求超时,弱网 UI 永久 loading | `src/lib/http.ts`(fetch 无 AbortController) | `AbortSignal.timeout(15000)`;幂等 GET 一次重试 |
| E11 | Google Fonts 境内不可达(目标市场!),品牌字体等于没上线 | `index.html:10-14` | 字体自托管(woff2 子集化) |
| E12 | 多租户隔离靠 handler 手写调用 ScopeService,新端点漏调无默认防护 | `server/src/business/scope.service.ts`(隔离本身默认拒绝、当前无漏洞) | 补"双租户越权矩阵" e2e(每端点互访必须 403/空集),把"当前正确"升级为"回归可证" |
| E13 | 入站 webhook 无时间戳重放窗口 + nonce 去重 | `youdao.controller.ts` | ±5min 窗口 + nonce 表 |
| E14 | PG 密码弱默认 `cps_pw` 无启动拦截(JWT 有黑名单、这个没有) | `docker-compose.pg.yml` | 改 `${POSTGRES_PASSWORD:?required}` |

### 2.2 产品旅程(摘自旅程走查,均已核实)

| # | 项 | 证据 | 动作 |
|---|---|---|---|
| J1 | 品牌入驻→首单链路断三处:向导提交后只有 toast、无"开门户账号→上架→接回传"指引;向导第 3 步不受控字段静默丢弃;门户 BrandOnboarding 只读无下一步 | `Brands.tsx:165,276-277,315`;`BrandPortal.tsx:167-186` | 入驻状态卡改 checklist(每步带跳转按钮);不受控字段要么落库要么禁输 |
| J2 | 门户/C 端零新手引导:CoachMarks 只挂控制台总览;门户才是新商户第一入口;guides.ts 缺 /products | `AppLayout.tsx:389-398`;`GuideDrawer.tsx:9-10` | ClientLayout 挂 CoachMarks(门户版 3 步)+ GuideDrawer 入口;补 /products 指南 |
| J3 | 空态无下一步动作:Orders/Settlement 空数组渲染 0 行表;EmptyState 无 action slot | `forms.tsx:99-112`;`kit.tsx:60-62` | EmptyState 加 CTA prop;零数据租户总览给"快速开始"清单 |
| J4 | 会话过期丢工作:refresh 失效弹回登录(深链回跳已做对),但表单内容丢、无"登录已过期"说明;OnboardWizard 点遮罩即卸载 3 步全丢 | `Login.tsx:27-29`;`overlays.tsx:155` | 登录页展示过期原因;脏表单关闭需确认(Modal 加 dirty 检测) |
| J5 | 跨端一致性靠手动刷新:全库无轮询/SSE;演示态主 store 无 storage 监听,多 tab 互相覆盖;演示态控制台上架永远不出现在 /market(marketDemo 模块级缓存) | `store.ts:236-246`;`marketDemo.ts:43-44` | 关键页 30s 轮询或最小 SSE;landing.ts 的跨 tab 回灌模式(storage 事件)铺到主 store |
| J6 | 合规文案零覆盖:全库无用户协议/隐私政策;C 端支付前无协议勾选;入驻无协议确认 | grep "用户协议\|隐私政策" 零命中 | 协议文档 + 支付/入驻勾选组件;ICP/支付牌照等线下项按 README:267 既有认知推进 |
| J7 | 通知只有站内(Bell 完整),无邮件/短信触达;结算出账/熔断等资金事件商户可能整天看不到 | Notification 表 + `ClientLayout.tsx:105-111` | 接一个邮件通道(资金事件必达);短信后置 |

### 2.3 设计系统 Quick Wins(1 天内可清完,全部点状)

| # | 项 | 证据(已核实) | 动作 |
|---|---|---|---|
| D1 | favicon 品牌红错版 `#FC011A` ≠ `#f5333b` | `public/favicon.svg`(实测) | 改正 |
| D2 | index.html 零 OG/description/theme-color,分享无卡片 | `index.html` 全文(实测) | 补 OG 三件套 + description + theme-color |
| D3 | 灰字对比度不达标:ink-4 白底 **2.31:1**(308 处)、ink-3 **3.42:1**(195 处)、白字on品牌红 **3.88:1**(数值经独立计算复核) | `index.css:15,14` | ink-4 提至 ≥3:1(约 #8a8f98)、ink-3 提至 4.5:1 线;**改 2 行令牌全站生效**——令牌架构的红利 |
| D4 | ForecastLine 网格线硬编码 `rgba(20,21,26,.06)`,暗色下消失 | `charts.tsx:574`(实测) | 改 `var(--color-line)` |
| D5 | AuthShell 两处硬编码 `#f5333b` 辉光 | `AuthShell.tsx:37-38`(实测) | 改 token |
| D6 | Button 缺 `danger` variant 与 `size` prop——这是 33 处手搓按钮的根因,连 Confirm 都在手搓危险按钮 | `primitives.tsx:281-324`;`overlays.tsx:204` | Button 补 danger/sm;Confirm 改用 |

### 2.4 动效 P1(最拉质感的一批)

| # | 项 | 证据(已核实) | 动作 |
|---|---|---|---|
| M1 | **懒加载卸壳整屏闪白**:唯一 Suspense 包在 Routes 外层,任何 lazy 路由首载时侧栏+顶栏一起消失 | `App.tsx:104`(实测) | AppLayout/ClientLayout 的 `<main>` 内各嵌一层 Suspense,fallback 用内容骨架;外层只留给无壳路由 |
| M2 | **全部 overlay 无退场动画**:Modal/Drawer `if (!open) return null`,关闭即闪没 | `overlays.tsx:104,152`(实测) | `useExitPresence(open, 200)` hook + reverse 动画,一个 hook 覆盖 Modal/Drawer/Confirm/CommandPalette 五处 |
| M3 | Toast 硬消失 + 堆叠跳位,无上限 | `overlays.tsx:61,66` | leaving 态 + 折叠补位;上限 4 条 |
| M4 | 支付按钮无 spinner、成功无编排(转化终点!) | `Supermarket.tsx:419`;`LandingPage.tsx:194` | 按钮 spinner(primitives.tsx:320 有现成模式)+ 勾章双段 draw + 纯 CSS 轻量 confetti(reduced-motion 隐藏) |
| M5 | 算价侧栏行增删无动画、件数徽章硬变 | `Supermarket.tsx:334,346-353` | 复用 .animate-row / leaving 折叠 / 徽章 key 重挂 + animate-pop |
| M6 | **动效 token 缺失**(所有修复的前置):14 档离散 duration、3 个 easing 家族混用、两套 useCountUp 并存(520ms vs 1000ms) | `index.css` 全文;`lib/useCountUp.ts` + `primitives.tsx:16`(实测) | @theme 增 `--ease-out/--ease-standard/--ease-spring/--ease-draw` + `--dur-fast/base/slow/draw`;合并 useCountUp 保留 primitives 版(有 epoch replay) |
| M7 | 杂项:`pulse-dot` 坏死类("实时"绿点是静止的,实测无定义)、骨架双体系(pulse vs shimmer)、TableSkeleton 行高 36px ≠ 表格 44px、hover 抬升量分裂(1px vs 2px)+ AgentPortal.tsx:88 双 translate 冲突、reduced-motion 下 animation-delay 未清零 | `Orders.tsx:161`;`primitives.tsx:180`;`kit.tsx:131`;`index.css:186` | 各一行级修复,随 M6 一批清 |

---

## 3. P2 —— 上线后迭代(收录备查,不排期)

**工程**:compose 弱默认改必填 / 双 schema 收敛 PG 单 schema / 补 Order[status]、AuditLog[at,actorId] 索引 + PG 部分唯一索引 / Prisma 慢查询钩子 / 账号级登录锁定 / body limit 显式化 / vendor manualChunks + hidden sourcemap + 图片 lazy(59 处 img 仅 1 处带 loading)/ marketDemo、adminDemo 静态 import 改动态 + CI 产物模式断言 / 发票税务字段 / 服务端导出端点(带审计)。

**产品**:真实模式 seed 兜底嵌套字段(新品牌顶着有道配置显示,store.ts:286,294)/ 门户 3 处英文枚举直出(BrandPortal.tsx:104,328,404)+ "%off" 中英混排 / AuthShell 品牌叙事硬编码"¥2.84亿"真实模式属虚假宣称 / 登出仅清本 tab / Supermarket 加载失败无重试按钮。

**设计(结构性,约 1 周)**:字阶收敛——864 处 `text-[..px]` 实际聚集为 5 档,命名 6 档 type scale 后 ~80% 可机械替换 / 33 处手搓 primary 按钮 + 38 处手搓标题清扫(依赖 D6 先行)/ Field 加 error prop + aria-invalid 全链路 / 四套空态并一 / ChartFrame(统一图表空态/loading/aria)+ AreaLine 补 hover(现状:同页有的图能摸有的不能)+ HTML 轴标层(消除 preserveAspectRatio="none" 的窄屏文字拉伸)/ `<Page>`/`<KpiGrid>` 骨架组件(20 页逐字复制的 KPI 栅格收编)+ md 断点补齐 / 宽表列优先级(hidden md:table-cell)先救 Orders/Settlement。

**动效(P2)**:路由切换 .page-in 统一入场 / CountUp 覆盖扩到 Orders 等页 KPI / AreaLine/Sparkline/Donut/Bars 补入场绘制 / Crosshair tooltip 120ms 跟随 + rect 缓存 / Popover 定位后 transition 入场 / 表格筛选 tbody fade / 事件流新行 .animate-row / saved 金额同步滚动。

**战略性(持续)**:抽 packages/ui + 令牌契约文档化(1320/1180 容器宽、间距节奏、白标三层覆盖)+ Storybook / Playwright 视觉回归矩阵(明暗 × 三端 × 375/768/1440——暗色走 data-theme 属性,截图矩阵是防"白底白字"复发的唯一自动化手段)/ axe CI + 对比度 lint / 宽表卡片化降级规范。

---

## 4. 升级路线图

```
第 1 周   P0 闭环(7 项,约 5–8 人日)
          ├─ Day1-2  P0-1/2/3:seed 开关 + migration 基线 + 备份闭环   ← 工程,互相独立可并行
          ├─ Day2-3  P0-4:登录门控 + 改密端点/UI + 首登强制改密
          ├─ Day3-5  P0-5:假 KPI 清理(Dashboard → Analytics → 其余)
          ├─ Day5    P0-6:离线 banner + 未接入集合空态
          └─ 决策项  P0-7:C 端对外与否(支付渠道依赖,或挂"即将开放"门禁)

第 2-4 周 P1 迭代(可三线并行)
          ├─ 工程线:E1-E14(webhook 重试、Sentry、Swagger 关闭、依赖升级、
          │         gzip/CSP、非 root、时区、超时、字体自托管、越权 e2e…)
          ├─ 产品线:J1-J7(入驻 checklist、门户引导、空态 CTA、脏表单确认、
          │         轮询/SSE、协议合规、邮件通道)
          └─ 体验线:D1-D6 Quick wins(1 天)→ M1-M7 动效 P1(3-4 天)
                    → 设计结构性清扫(字阶/按钮/空态/图表底座,1 周)

迭代期    P2 清单按业务优先级排 + 战略性基建(视觉回归/axe CI/packages/ui)
```

**验收门禁(每阶段完成的定义)**:
1. P0 阶段:生产镜像空库启动 → 无 demo 账号、migrate deploy 执行、备份 cron 在跑、真实模式无一处假 KPI、断网有 banner。
2. P1 阶段:双租户越权矩阵 e2e 全绿;Sentry 收到测试异常;Lighthouse 移动端 ≥85;axe 严重问题 0。
3. 体验线:明暗 × 三端截图矩阵 diff 通过;所有 overlay 有退场;动效参数全部走 token。

---

## 5. 已达标清单(有证据,勿重复投入)

**工程**:JWT fail-fast 黑名单校验(main.ts:12-26)/ pino 结构化日志 + 请求 ID 贯穿 + 5xx 带 requestId / prom-client metrics + /health /ready / 全局 120rpm + 登录 10rpm 限流 + 防枚举文案 / helmet / CORS 白名单 / 幂等服务(placeholder 行串行化 + 资源绑定)/ Decimal.js 银行家舍入 + splitProportional 残差吸收 / fail-closed 资金审计 30+ 动作含 before/after/IP/UA / ScopeService 默认拒绝 + scope 后覆盖客户端参数 / 出站 RSA 签名 + 密钥指纹单点收敛 / 双 schema CI 同步校验 / 前端 audit 0 漏洞。

**前端**:全局 ErrorBoundary 含 chunk-404 识别与自救刷新(优于多数生产项目)/ token 不落 localStorage(refresh 走 cookie)/ 401 静默刷新 + Web Locks 跨 tab 令牌旋转串行化 / 路由级 lazy(60+ chunk)/ 无 dangerouslySetInnerHTML / 登出清场区分模式。

**设计**:令牌三层架构(语义色三件套 × 明暗全量 × 白标覆盖)/ 统一品牌色 focus-visible / Modal 完整焦点陷阱 + 还原 / Toast aria-live / reduced-motion CSS+JS 双侧尊重 / 色盲友好(状态=色点+文字)/ Badge/PageHeader/ThresholdBar 等原语已被 30 页采用 / 三端 max-width 约束。

**产品**:门户取数三态机(loading/error+重试/空)/ 镜像写失败不静默(toast+回收真值)/ CoachMarks 克制 + GuideDrawer 覆盖 18 路由 / 演示剧本对真实模式已隐藏(Dashboard.tsx:146)/ 开发者中心(RSA 自助/联调/日志/健康分)是完整可交付能力 / TODO/FIXME 全库零残留。

---

## 附:评审方法说明

四个维度并行独立审计,每份报告的关键论断由主线**逐条回源码核实**后才收录本文——本轮核实实锤了全部 7 个 P0(含 entrypoint/seed/migrations 目录/备份脚本/Login 门控/改密 grep/kpi 常量/demoProgress 的逐一实测),并复算了对比度四组数值(2.31/3.42/3.88/3.28 与审计逐位吻合);此前两轮 review 中被核实程序剔除的 8 个误报(如 DEMO_RSA_PUBLIC 实为 seed/e2e 依赖)未混入本文。文中所有 file:line 均可直接跳转验证。
